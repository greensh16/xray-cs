name: Release

# Triggered when a version tag is pushed (e.g. v1.0.1).
# Tags should only be pushed after CI is green on main.
on:
  push:
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # ── Release binary builds ────────────────────────────────────────────────────
  build-release:
    name: Release binary (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux binaries are statically linked against musl so they run on
          # HPC login nodes and older distros regardless of the host glibc.
          # A gnu-linked binary built on ubuntu-22.04 requires glibc 2.35+,
          # which excludes RHEL/CentOS 7-8 and SLES 12 — still common at
          # facilities. Both use `cross`, whose images carry the musl toolchain
          # and linker; the artifact names are unchanged so `action.yml` and the
          # documented curl commands keep working.
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-musl
            artifact: xray-linux-x86_64
            cross: true

          - os: ubuntu-22.04
            target: aarch64-unknown-linux-musl
            artifact: xray-linux-aarch64
            cross: true

          - os: macos-14
            target: aarch64-apple-darwin
            artifact: xray-macos-arm64

          #- os: windows-2022
          #  target: x86_64-pc-windows-msvc
         #   artifact: xray-windows-x86_64.exe

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross (for Linux musl targets)
        if: matrix.cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Cache
        uses: Swatinem/rust-cache@v2
        with:
          key: release-${{ matrix.target }}

      - name: Build release binary (native)
        if: "!matrix.cross"
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build release binary (cross)
        if: matrix.cross
        run: cross build --release --target ${{ matrix.target }}

      - name: Stage binary (Unix)
        if: runner.os != 'Windows'
        run: |
          cp target/${{ matrix.target }}/release/xray ${{ matrix.artifact }}
          chmod +x ${{ matrix.artifact }}

      - name: Stage binary (Windows)
        if: runner.os == 'Windows'
        run: copy target\${{ matrix.target }}\release\xray.exe ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  # ── GitHub Release ────────────────────────────────────────────────────────────
  release:
    name: Create GitHub Release
    runs-on: ubuntu-22.04
    needs: [build-release]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all binaries
        uses: actions/download-artifact@v4
        with:
          path: dist/

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*
          generate_release_notes: true

  # ── crates.io publish ─────────────────────────────────────────────────────────
  #publish-crates:
  #  name: Publish to crates.io
  #  runs-on: ubuntu-22.04
  #  needs: [build-release]
  #  steps:
  #    - uses: actions/checkout@v4

  #    - name: Install Rust toolchain
  #      uses: dtolnay/rust-toolchain@stable

  #    - name: Publish
  #      run: cargo publish --token ${{ secrets.CARGO_REGISTRY_TOKEN }}
